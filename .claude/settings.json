{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p .claude && touch .claude/.pending-review"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"' 2>/dev/null); case \"$cmd\" in *'git commit'*) if [ -f .claude/.pending-review ]; then echo '⚠️ コードを変更しました。コミット前に /code-review を実行してください（red-team-reviewer による Adversarial Review が含まれます）。レビュー完了後、自動的にこのブロックは解除されます。' >&2; exit 2; fi ;; esac"
          }
        ]
      }
    ]
  }
}
